# Handles the cart_items variable


def cart_contents(request):
    """
    This is a contexts processor to make the 'context' dictionary available across the entire application
    """

    context = {}

    return context